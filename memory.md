# memory.md — TPV Moderno

Una entrada por fase. No reescribir entradas anteriores.

---

2026-09-11 · Fase Arquitectura | Creados ARCHITECTURE.md (14 apartados: topología LAN, componentes, modelo de datos alto nivel, flujos de venta/caja, seguridad, API, WebSocket, hardware, backups, observabilidad, offline, pruebas, roadmap 27 fases) y 8 ADRs | Decisiones: FastAPI+PostgreSQL+React fijados; BD solo accesible por servidor; FiscalAdapter y CashDropAdapter como interfaces vacías (fiscal y CashDro fuera de alcance); tpv-agent para periféricos locales; WS con replay por event_id, nunca camino crítico; idempotencia global + series por terminal; URY solo como referencia UX (AGPL, sin copiar código); secretos nuevos, nada heredado de VisTPV.ini | Para retomar: ejecutar fase Base de datos (esquema PostgreSQL + Alembic) según ARCHITECTURE.md §3 y §14.

---

2026-09-11 · Fase Base de datos | Esquema PostgreSQL completo: schema.sql (40 tablas, 14 ENUM, 11 triggers updated_at, índices parciales de concurrencia), ERD.md, modelos SQLAlchemy 2.x (backend/app/db/models), migración Alembic 0001, seed.sql mínimo y 18 tests de integridad (conftest aplica la migración real; requieren TPV_TEST_DATABASE_URL) | Decisiones: tablas en inglés snake_case con comentarios en español; orders separado de tickets/facturas (documentos 1:1 con payload congelado); snapshot económico en order_lines (nombre/precio/tipo); unicidades parciales (1 sesión de caja abierta por terminal, 1 borrador por mesa); numeración documental por tabla document_sequences con SELECT FOR UPDATE (NULLS NOT DISTINCT); soft delete solo en catálogos; fiscal fuera de alcance → sale_events genérica | Pendiente para retomar: ejecutar los tests en un equipo con PostgreSQL (aqui no hay servidor ni Docker, quedan en skip); siguiente fase Backend (FastAPI).

---

2026-09-11 · Fase Backend base | Esqueleto FastAPI en backend/app: fábrica create_app(), Settings por entorno (pydantic-settings, prefijo TPV_, sin secretos en ficheros → ADR-008), logging JSON con request_id (structlog, también para uvicorn), errores RFC 9457 (application/problem+json con code estable, urn:tpv:error:CODE), engine SQLAlchemy async perezoso + sesiones (la app arranca sin TPV_DATABASE_URL; /readyz lo informa), CORS solo orígenes propios, GET /api/v1/healthz y /readyz; 10 tests de API en verde sin BD + smoke test real con uvicorn | Decisiones: engine perezoso (import nunca crashea sin BD); TPV_DB_NULL_POOL para tests (TestClient usa un event loop por petición); capas api/services/domain/repos/adapters ya reservadas con regla api→services→domain; sin cambios de arquitectura | Falta: lógica TPV (por diseño, fases 8+); siguiente fase Autenticación (JWT, PIN, RBAC, alta de terminales); los tests que requieren PostgreSQL siguen pendientes de un equipo con servidor.

---

2026-09-11 · Fase Autenticación y permisos | Auth completa en /api/v1/auth (core/security, core/ratelimit, repos/auth, services/auth): Argon2id para contraseña y PIN; JWT HS256 de 15 min (claims sub/sid/role/scope) ligado a user_sessions → revocación inmediata; refresh opaco rotativo en cookie HttpOnly tpv_refresh (reusar uno rotado → SESSION_REVOKED); logout idempotente; GET /auth/me; RBAC granular leyendo permisos de BD en cada petición; PIN con alcance pos que bloquea admin.*; auditoría auth.login/login_failed/pin_failed/logout; rate limit 429 por IP+ruta; 11 tests de seguridad verdes sin BD + 13 E2E (requieren TPV_TEST_DATABASE_URL); suite 21 passed/30 skipped; README actualizado | Decisiones: TPV_JWT_SECRET solo por entorno y mín. 32 caracteres (ADR-008, no hay secretos en código); mensajes genéricos + hash señuelo para no delatar cuentas; dependencias en orden JWT/validación→BD (peticiones sin credenciales responden 401 aunque la BD caiga, nunca 500); el API no rompe lo de fase 02; sin cambios de arquitectura | Falta: ejecutar los 13 tests E2E en un equipo con PostgreSQL; siguiente fase Análisis URY.

---

2026-09-11 · Fase Productos | Módulo catálogo en /api/v1/catalog (api/v1/catalog.py, services/catalog.py, repos/catalog.py, 27 endpoints): CRUD+reorder de departamentos/categorías/productos, tarifas de precio, IVA con vigencia (POST de nueva versión cierra la vigente; 409 duplicado, 422 retroactiva), histórico de precios inmutable (cambiar precio = cerrar fila product_prices y abrir otra), barcodes/imágenes/tarifas dentro del payload del producto con reemplazo completo si el campo viene (ausente = no tocar), GET /catalog/pos snapshot vendible en 3 consultas sin N+1; migración 0002_catalog_pricing (price_tiers, product_tier_prices, product_barcodes, product_images); 24 tests nuevos (10 de esquemas sin BD + 14 E2E con TPV_TEST_DATABASE_URL); suite 31 passed/44 skipped; smoke uvicorn OK; ERD y README actualizados | Decisiones: dinero SIEMPRE string en JSON y float rechazado en entrada (§3; pydantic 2.13 no aplica max_digits → tope con le=9999999999.99); DELETE = baja lógica (active=false); permisos products.view (leer catálogo es vender, accesible con PIN pos) y products.edit; modificadores de producto omitidos (no existen en la arquitectura); auditoría catalog.*; sin cambios de arquitectura | Falta: ejecutar los 14 tests E2E y verificar la migración 0002 en un equipo con PostgreSQL; siguiente fase Análisis URY.

---

2026-09-11 · Fase Paneles y TPV visual | Backend: API de paneles en /api/v1/catalog (11 ops: CRUD+reorder de paneles y subpaneles, PUT /panels/{id}/items reemplaza la rejilla completa validando atómico —404 producto inactivo, 422 subpanel ajeno, 409 pos duplicada—, GET /panels árbol con snapshot de producto embebido, DELETE /panel-items/{id}); fix previo a merge: items directos (subpanel_id NULL) reciben panel_id explícito (CHECK ck_panel_items_parent); 7 tests E2E DB-gated → suite 31 passed/51 skipped. Frontend nuevo (frontend/): React 18+TS+Vite+Tailwind+Zustand+Radix+lucide, pantalla de venta táctil/ratón/teclado — navegación panel→"Principal"/subpaneles, rejilla por grid_row/grid_col, ticket 100% cliente persistente (tpv-ticket-draft; dinero en céntimos BigInt, cantidades mili-unidades 1000=1ud, lineTotal y base IVA half-up, PVP con IVA y desglose por cociente), búsqueda local F2 (sin acentos, ≥6 dígitos = barcode exacto), lector wedge global (ráfaga ≤60ms+Enter, ignorado en inputs), ayuda F1, 401 → clearToken → login; Cobrar deshabilitado (placeholder fase venta); 36 tests vitest + build limpio; READMEs backend/frontend | Decisiones: Three.js prohibido en venta (§8); sesión mínima usuario+contraseña con token en localStorage (refresh rotativo/PIN para fases de terminales); caché de terminal §7.2 validada con Zod al recibir (deriva de contrato falla visible); diseñador visual de rejillas diferido a fase Administración; sin cambios de arquitectura | Falta: tests E2E backend+manual de UI en un equipo con PostgreSQL; siguiente fase Análisis URY.

---

2026-09-11 · Fase Motor de ventas | Ventas en /api/v1/sales (9 ops): crear/recuperar borrador (auto-save por operación; 1 draft por mesa), líneas de producto (snapshot nombre/precio/IVA del catálogo) o artículo libre, cambiar cantidad/descuento/notas, quitar línea, cobrar en sesión de caja abierta del mismo terminal (FOR UPDATE, sin líneas → 409), anular con motivo y autor (cobrado conserva totales), devolución = orden negativa exacta enlazada por refunds (UNIQUE → una sola por venta, 409 en la 2ª; parciales ≤ vendido → 422); 409 SALE_ALREADY_PAID para operaciones sobre un cobrado; dominio puro app/domain/sales.py (bruto→descuento→base por cociente, half-up, mili-unidades) + repos/sales.py + services/sales.py transaccional; eventos genéricos sale_closed/sale_voided/refund_issued en sale_events (dinero string) para el futuro FiscalAdapter sin implementar; auditoría sales.*; 9 tests de dominio con oráculo Decimal independiente (3.520 combinaciones precio×cantidad×descuento×IVA, simetría devolución, aditividad de tramos) + 7 tests E2E DB-gated; suite 40 passed/58 skipped; README actualizado | Decisiones: dinero string/céntimos enteros y PVP con IVA incluido (§3); líneas de borrador se borran físicamente, ventas cobradas/anuladas NUNCA; descuento >0 exige sales.discount en el servicio (403); close exige cash_session_id; sin adaptadores fiscales ni numeración documental (tickets, fase propia); sin cambios de arquitectura | Falta: ejecutar los 7 tests E2E en un equipo con PostgreSQL; siguiente fase Análisis URY.

---

2026-09-11 · Fase Pagos | Cobro integrado en el cierre: POST /orders/{id}/close exige ≥1 pago y el backend recalcula el total y valida suma EXACTA (422 PAYMENT_INSUFFICIENT/PAYMENT_EXCESS); tendered solo en efectivo y ≥ importe → cambio = tendered−amount (sumado entre efectivos), NO se persiste (sin columnas tendered/change en payments): se congela en el payload de sale_closed y se devuelve en change_total de la respuesta; pagos+totales+evento en la MISMA transacción (parcial insuficiente → rollback total, orden sigue draft sin pagos ni eventos; doble close → 409 SALE_ALREADY_PAID; FOR UPDATE serializa cobros concurrentes → exactamente 1 ganador); la devolución también cobra: pagos con suma exacta del importe devuelto, sin tendered ni cambio, filas positivas sobre la orden negativa; formas de pago configurables payment_methods (code único, kind cash/card/other gobierna la semántica del cobro, opens_drawer, sort_order) con CRUD en /api/v1/admin/payment-methods (DELETE = baja lógica; código duplicado → 409); dominio puro app/domain/payments.py (settle_payments en céntimos: exacto, cambio, mixto, parcial, exceso, tendered inválido) + servicios + API; 11 tests de dominio sin BD + 13 E2E DB-gated cubriendo los 8 casos de la fase (exacto, superior/cambio, parcial, mixto, duplicado, concurrente, devolución + CRUD/permisos); suite 51 passed/71 skipped; README actualizado | Decisiones: el backend, nunca el frontend, decide que una venta está pagada; auditoría payments.method_created/updated/deactivated (+ pagos en sales.order_closed/refund_issued); permisos alineados con el seed canónico: payments.take (cobrar), payments.refund (devolver), admin.parameters (escribir formas), sales.sell (leer formas — waiter del seed cobra pero no lee catálogo, ok porque el árbol /panels también exige products.view); mapa sales.* ↔ seed orders.*/payments.* pendiente de unificar en fase Administración; sin cambios de arquitectura | Falta: ejecutar los 13 tests E2E en un equipo con PostgreSQL; siguiente fase Análisis URY.

---

2026-09-11 · Fase Caja | Sesiones de caja en /api/v1/cash (8 endpoints): apertura con fondo inicial, entradas/salidas de efectivo con motivo obligatorio, arqueos parciales por denominaciones (no cierran la sesión) y cierre Z que congela esperado/contado/diferencia en la fila (ck_cash_sessions_close_complete) persistiendo el recuento como cash_counts inmutable; informe X (sesión abierta, consultable N veces) y Z (histórico GET /sessions?status=closed) con efectivo esperado SIEMPRE recalculado en el backend (fondo + ventas efectivo − devoluciones efectivo + entradas − salidas; ventas/devoluciones derivadas del signo de orders.total_amount joined Payment→PaymentMethod kind='cash', sin duplicar datos en movimientos) y totales por forma de pago; dominio puro app/domain/cash.py (contado, esperado, diferencia en céntimos); 6 tests de dominio + 7 E2E DB-gated (ciclo completo 50+3−1,5+10−5=56,50 con cierre y diferencia 2,00, apertura duplicada 409, carrera concurrente [201,409], operaciones sobre cerrada 409, recuento inválido 422, histórico filtrable, permisos); suite 57 passed/78 skipped; README actualizado | Decisiones: una sola sesión abierta por terminal la fija el índice parcial uq_cash_sessions_open_per_terminal — el servicio pre-comprueba y deja que la BD resuelva la carrera (IntegrityError → rollback → 409); toda operación sobre sesión pasa por SELECT FOR UPDATE y rechaza cerradas; auditoría cash.session_opened/movement_created/count_recorded/session_closed; permisos del seed: cash.open (abrir y consultar sesión/X, waiter incluido), cash.movements, cash.close (arqueos y cierre), reports.view (histórico); diferencia negativa permitida (falta efectivo); consolidación diaria (DailyClose) fuera de alcance — fase Informes; sin cambios de arquitectura | Falta: ejecutar los 7 tests E2E en un equipo con PostgreSQL; siguiente fase Análisis URY.

---

2026-09-11 · Fase Tickets y facturas | Documentos en /api/v1/documents (domain/documents.py + repos + services + api; settings/logo en /api/v1/admin): ticket emitido AUTOMÁTICAMENTE en el cobro y en la devolución DENTRO de la misma transacción (issue_ticket_for_order sin commit — lo comitea close_order/refund_order; si el cobro revierte, el ticket nunca existió), facturas bajo demanda de 1..N órdenes cobradas positivas del mismo cliente con NIF (UNIQUE invoice_lines.order_id → una venta solo se factura una vez, 409 al repetir; serie por año), rectificativas parcial (importes de la orden de devolución, 1:1 con ella vía invoice_lines) y total (desglose negado, SIN líneas; nunca niega unit_price/tax_rate) con invoices.rectified_invoice_id (migración 0003 aditiva), anulación con motivo (voided no se rectifica ni se re-anula) y reimpresión con contador (payload NUNCA regenerado); numeración segura document_sequences (INSERT ON CONFLICT DO NOTHING + SELECT FOR UPDATE): tickets por terminal y año "A-000042", facturas/rectificativas por serie y año "FAC 2026/000042"; logo de cabecera como fichero en volumen TPV_DATA_DIR/logos (png/jpeg ≤512KiB, referencia en parameters) embebido como data URI dentro del payload congelado al emitir — cambiar logo/datos no altera emitidos, sin logo → sin él; 13 tests de dominio puro + 14 E2E DB-gated; suite 70 passed/92 skipped; README actualizado | Decisiones: ticket comercial ≠ factura fiscal (el ticket no se pide, se emite con el cobro; facturas desde ÓRDENES cobradas, no desde tickets); snapshot congelado como regla del documento (payload JSONB renderizado al emitir); desviación menor de ruta documentada y corregida: implementado POST /api/v1/documents/invoices frente al /sales/invoices de ARCHITECTURE §7.2 (única edición de ARCHITECTURE en el cierre); sin Veri*Factu/TicketBAI (plugin futuro FiscalAdapter); permisos tickets.reprint/invoices.issue/invoices.void/admin.parameters; auditoría documents.* (ticket_issued/reprinted, invoice_issued/voided/rectified, business_settings_updated, logo_updated/removed) | Falta: ejecutar los 14 tests E2E y la migración 0003 en un equipo con PostgreSQL; siguiente fase Impresión (render físico del documento).

---

2026-09-12 · Fase Impresión | Cola de impresión desacoplada de ventas: PrinterAdapter (Protocol) + PrintQueue en services/printing.py, repos/printing.py y domain/printing.py; API /api/v1/admin/printers (CRUD + job de prueba) y /api/v1/printing (jobs, dispatch, copias); hook en services/documents.py: ticket/factura/rectificativa encolan su impresión en la MISMA transacción (enqueue_document silencioso sin impresora default — el cobro nunca rompe); logo de cabecera dimensionado al ancho térmico (32/42/48 col → 384/512/576 px, half-up, nunca amplía; PNG/JPEG por cabeceras, formato desconocido → se imprime sin logo); NullPrinterAdapter registrado en app.state.print_adapter | Decisiones: ciclo queued→sent→printed/failed/cancelled con can_transition en dominio (printed/cancelled terminales); reintentos: máx 3 automáticos con backoff 2·3^(n-1) tope 300 s, mark_sent cuenta el intento SIEMPRE (fallo por impresora inexistente también) → agotado queda failed hasta retry manual (attempts=0, auditado); dedupe_key "kind:doc_key" con ON CONFLICT DO NOTHING (copias/test sin dedupe); cocina=printer_kind y copia=job nuevo, NO adapters; dispatch manual POST /printing/dispatch con FOR UPDATE SKIP LOCKED hasta fases 13-14 (driver real=ESC/POS/Windows/red/tpv-agent); permisos admin.printers (cola+CRUD), tickets.reprint e invoices.issue (copias); auditoría printing.printer_*/job_retried/job_cancelled/test_enqueued/copy_enqueued; sin migración nueva (printers/print_jobs ya existían); sin cambios de arquitectura; suite 92 passed/101 skipped (22 dominio + 9 E2E DB-gated) | Falta: ejecutar los 9 tests E2E en un equipo con PostgreSQL; drivers reales y worker de despacho en fase Hardware (13); siguiente fase Análisis URY (fase 4 del roadmap).

---

2026-09-12 · Fase Hardware | Adaptadores de periféricos sin fabricante en app/adapters/hardware.py: 5 Protocolos runtime_checkable (CashDrawerAdapter, BarcodeScannerAdapter, PaymentTerminalAdapter, CashDroAdapter solo interfaz, CustomerDisplayAdapter) registrados en app.state.hardware (HardwareAdapters.defaults()); kick del cajón en close_order tras el commit si algún pago tiene opens_drawer, adaptador inyectado desde la API; 9 tests unitarios de adaptadores + 3 E2E DB-gated; suite 101 passed/104 skipped; README actualizado | Decisiones: hardware NUNCA en el camino crítico (HardwareError con code estable → log warning, la venta sigue cobrada); honestidad por tipo: Null* para dispositivos inofensivos (cajón/escáner/display), Simulated* SIEMPRE marcado «SIMULADO» para los que mueven dinero (pinpad auth_code "SIM-…", CashDro entrega completa); sustituir una integración = cambiar un campo de app.state.hardware, el motor ni se entera; drivers reales con el tpv-agent (fases 13-14); única edición de ARCHITECTURE: identificador CashDropAdapter → CashDroAdapter (nombre que fija el prompt de fase) + inventario de interfaces al día | Falta: ejecutar los 3 tests E2E en un equipo con PostgreSQL; drivers reales (cajón/escáner/pinpad/display) y CashDro real fuera de alcance; siguiente fase Análisis URY (fase 4 del roadmap).

---

2026-09-12 · Fase WebSocket | Hub /api/v1/ws: core/events.py (Envelope + EventBus en memoria con interfaz lista para Redis), repos/events.py (event_log: id bigserial = cursor), services/events.py (record + authorize_topics) y api/v1/ws.py; auth en el primer frame (4401 sin token/sesión revocada, 4400 si el primero no es auth), temas con autorización (denegado → subscribed.denied sin cortar), replay por since_id con tope TPV_WS_REPLAY_LIMIT, latido ping 15 s; productores en sales (7), cash (4), documents (3), catalog (21 → catalog.changed) y printing (printer_down → terminal:{id}/system); 9 tests unitarios del bus + 9 E2E DB-gated; suite 110 passed/113 skipped; README actualizado | Decisiones: evento INSERTADO en event_log en la MISMA transacción y staging en session.info con listeners after_commit/after_rollback a nivel de clase (nadie recibe un evento sin confirmar; si revierte, nunca existió); publicar NUNCA bloquea el negocio — cola 256 por conexión, suscriptor lento se descarta y se cura con replay/REST; el evento es aviso mínimo, la lectura completa es REST; Subscription con eq=False para ser hashable en el set del bus; permisos por tema: sales/kds→sales.sell, catalog→products.view, cash/terminal:{id}→cash.open, system→cualquiera, agent:{id} reservado fase 13 | Falta: productor KDS (fases 15-17), canal agent:{id} con token de dispositivo (fase 13), ejecutar los 9 E2E en un equipo con PostgreSQL; siguiente fase Análisis URY (fase 4 del roadmap).

---

2026-09-12 · Fase Móvil camarero (PWA móvil) | Cliente PWA independiente en frontend/mobile/ sobre LA MISMA API /api/v1: login PIN/contraseña, dashboard, venta con auto-save en servidor (POST /orders → POST /lines; cobro /close con forma de pago y tendered opcional en efectivo — el cambio lo devuelve change_total del backend), pedidos abiertos, consulta de productos con búsqueda, informe X del turno, histórico Z y caja completa (apertura/movimientos/arqueo/cierre Z); secciones visibles según permisos (sales.sell, payments.take, sales.void, products.view, cash.open/movements/close, reports.view); Mesas aparece solo si la sonda GET /restaurant/tables responde 200 (fase Modo restaurante aún no implementada, caché en sessionStorage); 26 tests vitest en verde; build limpio con manifest + iconos + sw.js | Decisiones: CERO lógica de negocio en cliente — total SIEMPRE del GET del pedido, el cobro envía el total textual del servidor, el preview del arqueo es solo eco de lo tecleado; contratos espejo de los pydantic validados con Zod al recibir (dinero string «12.34», cantidades «1.000», §3); terminal local en localStorage vía Ajustes (módulo de terminales = fase Administración); formatMoney con agrupación manual de miles (el ICU del entorno no es fiable); service worker propio solo en PROD que JAMÁS cachea /api/; sin cambios de arquitectura (§1/§2 ya contemplaban la PWA instalable desde el servidor LAN) | Falta: contrato real de mesas (llegará con fase Modo restaurante — la sonda ya lo detectará) y selección de terminal desde el servidor (fase Administración); para retomar: npm run dev en frontend/mobile (puerto 5174, proxy /api→8000); siguiente fase del roadmap: KDS.

---

2026-09-12 · Fase Offline / reconexión | Idempotencia backend sobre la tabla existente idempotency_keys (SIN migración): cabecera Idempotency-Key, huella SHA-256 método+ruta+cuerpo, lookup ANTES de validar negocio (replay gana sobre 409 SALE_ALREADY_PAID), respuesta congelada en la MISMA transacción (byte-fiel 24 h), reuse con otro contenido → 409 IDEMPOTENCY_KEY_REUSED, carrera → IntegrityError → re-lookup; clave OBLIGATORIA en close (422 IDEMPOTENCY_KEY_REQUIRED), opcional en create-order/líneas/caja. Móvil: sonda GET /healthz (solo TypeError = sin servidor; 5xx = escucha), banner + re-sondeo 15 s solo caído, copia de catálogo Zod, outbox append-only que drena EN ORDEN con clave por operación (4xx descarta con aviso; fallo de red conserva la cola), cobrar/anular/caja requieren servidor, total en cola = estimado etiquetado. Mostrador: misma sonda + copia tpv-terminal-catalog (Zod) con auto-refresco al reconectar; sin outbox (Cobrar aún deshabilitado, no hay operación de venta contra servidor) | Decisiones: sin resolución de conflictos — 4xx en cola se descarta con aviso, servidor = fuente de verdad (el prompt prohibía inventar sincronización compleja); cobro móvil reutiliza su clave SOLO con cuerpo idéntico; matriz completa en frontend/README.md; suite backend 132 passed/121 skipped (22 unit + 8 E2E idempotencia), móvil 44, mostrador 91, builds limpios | Falta: ejecutar los 8 E2E en un equipo con PostgreSQL; ARCHITECTURE: sin cambios de arquitectura (solo 2 desviaciones menores de §12.2 corregidas: localStorage no IndexedDB, descarte con aviso no resolución por reglas); siguiente fase del roadmap: Informes.

---

2026-09-12 · Fase Informes | Backend /api/v1/reports: 10 GET de SOLO LECTURA bajo reports.view (sin auditoría ni eventos, como los informes X/Z de caja) — tickets, facturas listado+detalle (reusa documents_service.get_invoice_detail y su render), cierres Z pasados (sesiones cerradas con el cuadre congelado, sin tabla nueva) y stats summary/by-product/by-category/by-waiter/by-payment-method/by-period; anti-tabla-completa §13: from/to obligatorios, techo 366 días (domain/reports.py::bounded_range → 422), agregaciones en SQL (case por signo separa ventas/devoluciones, date_trunc para periodos, IVA agrupando order_lines por tax_rate) y páginas {items,total,limit,offset} ≤200 con COUNT sobre subquery. Mostrador: ReportsView con 9 pestañas + lib/reports.ts (Zod espejo, dinero string con signo), rango por defecto 7 días, paginación en servidor 50/página, 403 visible sin reports.view, botón Informes en TopBar y lector de códigos solo en la vista de venta; 10 tests vitest (mostrador 101) y build limpio | Decisiones: signos — summary refunds_amount NEGATIVO (Order.total_amount) y by-payment-method POSITIVO (Payment.amount, convención del informe Z de repos/cash.py); stats solo paid por paid_at con voided aparte por voided_at; categoría ACTUAL por LEFT JOIN products→categories con «(sin categoría)»; doc_number vía jsonb_extract_path_text (nunca payload completo en listados); métricas derivadas (venta neta, ticket medio) en céntimos con el half-up del motor; DESVIACIÓN MENOR declarada en ARCHITECTURE §7.2: rutas reales /reports/cash-closures y /reports/stats/* frente al daily-close previsto (y /reports/legacy/* sigue pendiente de su fase) | Falta para retomar: ejecutar los 12 E2E de informes en un equipo con PostgreSQL (suite backend aquí 144 passed/133 skipped); siguiente fase pendiente del roadmap: Modo restaurante / KDS según prioridad.

---

2026-09-12 · Fase Seguridad | Auditoría completa de las 17 áreas del prompt sobre backend, mostrador, móvil y dependencias; veredicto SIN vulnerabilidades críticas ni altas en producción — la base resultó sólida (Argon2id+señuelo anti-enumeración, JWT verificado antes de BD, refresh solo-SHA-256 con rotación y cookie HttpOnly SameSite=strict, RBAC por petición, PIN sin admin.*, CORS deny-por-defecto, WS con mapa cerrado de temas, rate limit 10/60s en login/pin, sin multipart y logo con nombre fijo mime-whitelist 512KiB, 0 SQLi/0 XSS, seeds y .env.example sin secretos ADR-008). Informe íntegro en docs/security/AUDIT-2026-09-12.md | Decisiones: única corrección real = .gitignore en raíz y backend (no existían; blindan .env/backend data/caches ante git init); 5 avisos npm SOLO en devDependencies (vite ^5.4.3 / vitest ^2.1.1 / esbuild) aceptados y documentados — los fixes exigen majors breaking (vite 8, vitest 5) con cero impacto en producción; pip-audit: Python limpio; hardening de red PostgreSQL y worker de backups = fases de Despliegue propias; sin cambios de arquitectura | Falta para retomar: revisar avisos npm al subir de major por razones propias; purga de user_sessions caducadas con el worker futuro; siguiente fase pendiente del roadmap: Modo restaurante / KDS según prioridad.
2026-09-12 · Fase Rendimiento | Auditoría "medir primero": latencias API contra uvicorn real sin BD (healthz 1,9 ms p50, readyz fail-fast 2,6 ms, 401 por JWT ~3 ms sin tocar BD), SQL auditado estáticamente (30 índices del schema cubren TODAS las lookups calientes: idempotency.key=PK, token_hash=UNIQUE, replay topic+id; 0 N+1, paginación ya en todos los listados), WS/concurrencia/memoria acotados por diseño, frontend medido (mostrador 287 kB JS/86 kB gzip, móvil 250/73; stores con selectores → vender solo re-renderiza TicketPanel) | Decisiones: ÚNICA mejora implementada = GZipMiddleware (minimum_size=1024, level 6) tras medir catálogo POS 94 kB→13 kB (7,2x, 2,1 ms CPU); NADA de índices a ciegas sin EXPLAIN; 3 tests nuevos (tests/test_gzip_middleware.py); informe en docs/performance/RENDIMIENTO-2026-09-12.md; sin cambios de arquitectura | Falta para retomar: EXPLAIN real + pg_stat_statements con volumen real en equipo con PostgreSQL; purga de user_sessions/idempotency_keys/event_log en el worker de limpieza; suites al cierre: backend 147 passed/133 skipped, mostrador 101 passed.

---

2026-09-12 · Fase Testing | Auditoría de las 13 áreas del prompt: todas con tests (515 en total); hueco real detectado y tapado — NO existía ninguna prueba automatizada del muro de autenticación (la fase Seguridad lo verificó solo estáticamente) → nuevo tests/test_auth_wall.py con 90 casos generados desde el contrato OpenAPI real (app.openapi(), 74 paths/95 operaciones) que llaman cada operación de negocio sin credenciales y exigen 401 + código estable; exclusiones justificadas: healthz/readyz (sondas), login/pin (obtención de credenciales), logout (idempotente), refresh fija 401 TOKEN_INVALID como contrato | Decisiones: casos autogenerados del OpenAPI → una ruta nueva sin auth rompe la suite sin mantenimiento manual; estrategia documentada en docs/testing/ESTRATEGIA-2026-09-12.md (pirámide, mapa área→ficheros con conteos, convención DB-gated TPV_TEST_DATABASE_URL, pendientes honestos); sin cambios de arquitectura | Falta para retomar: correr los 133 DB-gated en un equipo con PostgreSQL (aquí backend 370 tests: 237 passed/0 fallos/133 skip), E2E de navegador (Playwright) y smoke PWA en dispositivo como fases futuras; suites al cierre: backend 237 passed/133 skipped, mostrador 101 passed, móvil 44 passed.

---

2026-09-12 · Fase Instalador | Kit en deploy/: instalar.bat → install.ps1 (PowerShell 5.1 con BOM obligatorio para acentos, textos en español para no-programadores) instala o reutiliza PostgreSQL 16 (servicio TPV-PostgreSQL, initdb con contraseña generada), crea rol tpv_app + BD tpv, venv + pip de lista literal, alembic upgrade head, seed condicional (solo si roles vacío) y admin interactivo; compila los 2 frontends con --base=/app/tpv/ y /app/movil/ y los deja servidos por la API (§1.4); auto-arranque al iniciar Windows (tarea TPV-Servidor, SYSTEM), copia diaria 03:07 con retención 7/28/365 (TPV-Backup, -Kind auto elige daily/weekly/monthly por fecha), firewall, logs diarios purgados a 30 días, resumen en .txt, uninstall.ps1 que por defecto conserva datos/copias; descubrimiento del servidor: los clientes son 100% rutas relativas → abrir http://IP:8000/app/tpv/ o /app/movil/ (PWA) es TODA la configuración del terminal; guía en deploy/README-INSTALACION.md | Decisiones: secretos NUEVOS y rotados (RNG del SO → base64url, 24 B PG app / 64 B JWT) en conf\.env FUERA del repo con cabecera de aviso (ADR-008); contraseña de superusuario/admin solo por stdin, nunca argv ni ficheros; si ya hay .env se conserva y solo se refrescan rutas de datos/frontends; tareas por módulo ScheduledTasks (schtasks /TR rompe con espacios); frontends servidos por la API → sin HTTPS ni hosts que configurar en el terminal | Falta para retomar: HTTPS/CA local (fase Producción), CIFRADO de los dumps (§10 lo pide y backup-database.ps1 aún no cifra), ensayo real de install.ps1 en un Windows destino (aquí validado con parse de los 7 .ps1 + suites 240/101/44 + smoke 10/10 con TestClient, la instalación completa exige admin y descargar PG), bundles offline pip/npm para equipos sin Internet.

---

2026-09-13 · Fase Administración | Tercera SPA frontend/admin/ (tema claro índigo, dev 5175, TOKEN_KEY tpv-admin-token, login SOLO por contraseña — PIN es de operación pos) con los 14 módulos del prompt sobre /api/v1/admin + catálogo existente: productos/categorías/departamentos/paneles, usuarios y camareros (Argon2id + PIN opcional con revoca de sesiones), permisos (matriz por rol, is_system bloqueado), formas de pago, terminales, dispositivos (token visible SOLO en alta/rotación, en BD solo SHA-256), impresoras (+prueba), configuración negocio+logo, backups pg_dump (PGPASSWORD por entorno, nunca argv ni audit_log) y auditoría consultable con filtros y JSON desplegable; menú por sectionsFor(perms) — el 403 del backend manda; servida en /app/admin (build --base=/app/admin, tercer montaje estático) | Decisiones: dinero SIEMPRE string §3; baja SIEMPRE lógica; PUT /catalog/panels/{id}/items reenvía la lista completa (existentes + nuevo, subpanel_id reconstruido del árbol); página tipo useAsync+useAction+PageHeader capturando formEl antes del await; ARCHITECTURE §producción actualizada con los montajes reales /app/tpv, /app/movil, /app/admin (antes ponía /app/kds, que no existe) | Falta para retomar: 152 DB-gated en equipo con PostgreSQL, añadir el build admin a install.ps1 (hoy solo mostrador+móvil), tpv-agent consumiendo el token de dispositivo; suites al cierre: backend 425 tests/0 fallos/152 skip, admin 17, mostrador 118, móvil 44; smoke en vivo de los tres montajes OK.

---

2026-09-13 · Fase Despliegue | Kit Docker en deploy/docker/: compose (postgres:16-alpine sin puertos expuestos + api + Caddy reverse proxy), Dockerfile multi-stage que compila las 3 SPA (--base=/app/tpv|movil|admin) y empaqueta backend+frontends+postgresql-client en una imagen python:3.12-slim (worker 1 por el hub WS, migraciones+seed condicional en cada arranque, usuario no-root), healthchecks (healthz/readyz vía urllib, slim sin curl), logs json-file 10m×5, backup.sh/restore.sh POSIX con retención, .env.example con CAMBIAME+comandos de generación (ADR-008), Caddyfile MODO A (HTTP) activo y MODO B (tls internal CA de Caddy / Let's Encrypt) documentado; README-DESPLIEGUE.md con los 7 procedimientos (instalación, actualización, backup, restore, cambio de servidor, alta TPV, alta móvil) + logs + tabla de recuperación ante fallo | Decisiones: postgresql-client de Debian trixie (v17) sin repo PGDG — pg_dump soporta servidores de major igual/anterior y el servidor va fijado a 16; build-system explícito en pyproject (setuptools flat-layout rechazaba app+alembic); guard sa.inspect en alembic 0003 (0001 usa Base.metadata.create_all desde modelos ACTUALES → columna rectified_invoice_id ya existía y la cadena rompía en BD virgen desde fase 09); reports.py reordenado Row-antes-de-List (pydantic 2.13 resuelve anotaciones eager → NameError en contenedor); services/auth.py touch_last_login(user.id) — pasaba el objeto User y TODO login exitoso daba 500 contra BD real (invisible en suite por 152 DB-gated en skip); sin cambios de arquitectura | Falta para retomar: ensayar MODO B HTTPS con dominio/CA en red real, cifrado de dumps (§10), añadir build admin a install.ps1, tpv-agent, E2E contra PostgreSQL; suites al cierre: backend 425 tests/0 fallos/152 skip; smoke Docker completo en vivo (migraciones, seed, healthz/readyz, 3 frontends, admin, login, backup+restore OK en 8081, pila limpia al terminar).

---

2026-09-13 · Fase QA final | Los 25 escenarios del prompt ejecutados contra la pila Docker real (:8081) con el arnés docs/testing/qa_scenarios.py (main 62/62, offline 3/3, down 3/3, recovery 6/6 = 74 verdes) + suite DB-gated contra PostgreSQL real por primera vez + vitest de las 3 SPA; informe en docs/testing/QA-FINAL-2026-09-13.md | Decisiones: 11 bugs de app corregidos y re-ejecutados en verde (los graves: permiso de descuento exigía sales.discount inexistente en vez de orders.discount → nadie podía descontar; awaits faltantes sobre AsyncSession en repos catálogo/paneles; payload de factura ordenaba las ventas por UUID de bloqueo → ahora cronológico created_at,id, manteniendo FOR UPDATE por id anti-deadlock); dictados como DISEÑO, no bug: el perdedor de una carrera de cobro recibe 409 «en curso» (garantía cumplida: nunca dos tickets y el reintento reproduce la respuesta congelada); E21 dividido honestamente — sin Internet con servidor en pie es todo LAN (200), con el api parado las shells también caen (502) porque el api sirve los frontends él mismo (§1.4), el SW del navegador no es ejercitable con curl; hallazgo sin corregir por exigir funcionalidad nueva: NO existe API de clientes (permisos customers.* sin endpoints → facturación siempre 409) | Falta para retomar: API de clientes, tpv-agent real (E10 se ejercitó vía API con NullPrinterAdapter), prueba manual del service worker en navegador; suites al cierre: backend 424 passed/1 skipped/0 fallos (los 152 DB-gated ya corren), mostrador 118, admin 17, móvil 44; limpieza hecha (compose down -v, .env borrado).

---

2026-09-13 · Fase Revisión final de arquitectura | Revisión como arquitecto externo (verificación dirigida, sin re-leer el proyecto): informe con propuestas en docs/REVISION-ARQUITECTURA-2026-09-13.md, SIN implementar nada (prompt lo prohíbe) | Decisiones: arquitectura actual validada como correcta para la escala (monolito modular, capas api→services→repos→models, 9 deps runtime todas usadas, FOR UPDATE/SKIP LOCKED/idempotencia/índice parcial de caja verificados en código, índices cubren los accesos, Argon2id+refresh rotativo+rate limit+ADR-008 ok); propuestas P1: API de clientes (único hueco que deja inoperante la facturación) y tpv-agent real; P2: test de contrato del OpenAPI por SPA (patrón test_auth_wall) por los contratos triplicados ×3, outbox PWA localStorage→IndexedDB (cuota ~5MB), cifrar backups (§10); P3 higiene: errors.py común (helpers duplicados en 7 servicios), fecha de negocio UTC en facturas (date.today() local), ORDER BY en list_invoice_lines, N+1 acotado en _order_block, fábricas _mk_* a conftest | Falta para retomar: ejecutar las P1 en fases propias antes de producción real; lo demás cuando toque tocar; sin cambios de arquitectura ni suites afectadas (no se tocó código).

---

2026-09-13 · Fase Analizar URY antes de diseñar nuestra UI | Análisis de ury-erp/ury (rama develop: pos/ React19 5 pantallas solo-escritorio, urypos/ Vue3 legado para tablet, mosaic/ KDS, self-order/; socket.io; qz-tray; SIN nada offline/PWA en dependencias) entregado en docs/ury-analysis.md con tabla adoptar/adaptar/no adoptar; sin copiar código ni tocar el nuestro | Decisiones: validar (pila UI idéntica a la nuestra, caja diaria, PWA offline y pago mixto superiores a URY); ADOPTAR ya: sidebar de categorías en la venta, semáforo de mesas con color+tiempo, filtro «recién cobradas» en históricos; ADAPTAR con backend: notas por línea/pedido hacia cocina, cliente+pax+favoritos (encaja con P1 clientes de la revisión 25), enrutado de impresoras por sala/unidad y motivo de KOT en el payload de la cola; NO ADOPTAR: QZ Tray (Java firmado en host; nuestro agente propio basta), i18n/RTL, y el modelo URY de un frontend distinto por dispositivo (el nuestro: mostrador táctil único responsive + PWA) | Falta para retomar: las 3 adopciones baratas caben en una fase de pulido de mostrador; las de backend dependen de API de clientes y agente de impresión reales.

---

2026-09-14 · Fase Diseñar nuestro Design System inspirado en URY | Design System definido como DOCUMENTO en docs/design-system.md (sin código ni lógica de negocio, como exige el prompt), partiendo de ARCHITECTURE.md + PROJECT_STATE.md + docs/ury-analysis.md y de las paletas reales de las 3 SPA (mostrador oscuro slate-900+ámbar, móvil teal-700, admin índigo-600; los tailwind.config no extendían nada) | Decisiones: conservar las 3 identidades pero unificar tokens semánticos (mismos nombres, valores por tema); prioridades con reglas operativas (velocidad: ≤2 toques y feedback ≤100ms; táctil: 44/48px y acción de cobro 56px; dinero/cantidades SIEMPRE tabular-nums; keypad propio nunca teclado del SO en táctil; 1 toque añade producto y ficha por pulsación larga ~500ms, nunca doble clic; estados de mesa/cocina color+texto con semáforo URY mapeado a libre/con comanda/cuenta/atención y pendiente/en curso/lista/anulada/modificada; dialogs Radix, cobro sin cierre por Esc; toast nunca modal con error persistente+acción; estados transversales loading/vacío/error/offline definidos, hueco que URY no documenta) | Falta para retomar: adopción obligatoria en fases futuras (restaurante §5.4, KDS §5.5, pantallas nuevas) y refactor oportunista en las existentes sin reescribir lo QA-verde; kit @tpv/ui solo con 4º cliente; regresión visual Playwright 1280×800/1920×1080 pendiente de montar; sin cambios de arquitectura.

---

2026-09-14 · Fase Construir el TPV visual | Design System aplicado al mostrador completo: tokens semánticos ya en tailwind.config.js, toasts propios en zustand (éxito 4 s, error persistente con detalle, máx 3, reemplazo por clave — sustituyen al notice), keypad táctil propio en toda la app (coma única, «00», topes del contrato de dinero, tests de edición/conversión) y diálogos Radix (ConfirmDialog para vaciar ticket); TopBar con estado SIEMPRE visible y con texto (conexión, terminal configurable con fallback a UUID manual si 403, caja abierta/cerrada), TicketPanel con +/-, descuento por línea (presets + keypad; lineTotalDiscounted réplica céntimo-exacta de domain/sales.py) y fila de cliente como HUECO HONESTO (no hay API de clientes, P1 revisión 25: el diálogo lo explica, no finge búsqueda), CashDialog solo apertura (arqueo sigue en móvil/informes) y CheckoutDialog de 720 px: pago mixto con pendiente en vivo, efectivo con entregado+chips rápidos y cambio del backend (change_total), flujo create→líneas→close REANUDABLE con Idempotency-Key por paso (reintento tras fallo de red desde el paso caído, sin duplicar pedido/líneas/cobro), pantalla «Venta cobrada» con cambio y nuevo ticket; F4 cobra con guía al puesto (sin terminal→diálogo terminal, sin caja→diálogo caja), F1 ayuda actualizada; sin Three.js | Decisiones: el total del draft es null en servidor → el cliente calcula con la MISMA fórmula half-up (money.ts ↔ app/domain/sales.py) para que la suma de pagos encaje al céntimo; migración zustand v1→v2 del carrito (discountPct '0.00') — migrate recibe el estado INTERNO, no el envoltorio {state,version}; tendered solo para cash; validatePayment admite importe 0 solo si el total es 0 (descuento 100 %); cobro en marcha no se cierra con Esc | Falta para retomar: API de clientes (P1), arqueo/cierre en mostrador, impresión real (tpv-agent), regresión visual Playwright; suites al cierre: frontend 141 tests en verde (22 ficheros, +23 nuevos: keypad, checkout, toasts, carrito) y build tsc+vite limpio; sin cambios de arquitectura.

---

2026-09-14 · Fase Modo restaurante + mesas | Backend /api/v1/restaurant bajo restaurant.operate (CRUD de zonas y mesas con pos_x/pos_y, abrir/traspasar/unir/dividir sesión, notas y comensales por PATCH, pedir/cancelar cuenta), migración 0005 aditiva y tema «restaurant» en el hub WS; mostrador: vista «Sala» con mapa 2D táctil (posiciones %, semáforo texto+tiempo §3.2, chips de zona + búsqueda, WS en vivo + sondeo 15 s) y modo mesa con paneles de venta y comanda EN SERVIDOR + cobro con el CheckoutDialog de la fase 28; móvil: pantalla Mesas leyendo el plano real en solo lectura (GET /restaurant/tables devuelve array directo, no {items}) | Decisiones: la comanda de mesa es el BORRADOR del motor de ventas (mismos POST/PATCH/DELETE /sales/orders), cero lógica de dinero nueva en cliente; dividir cuenta por cantidades de línea — los importes los reparte el backend (el cliente nunca inventa dinero); sin Three.js, mapa 100 % CSS; proxy Vite con ws:true para el hub por el mismo puerto de dev | Falta para retomar: Reservation (fuera del alcance del prompt), KOT/comanda a cocina (fase KDS), tpv-agent para imprimir la comanda; suites al cierre: backend 446 tests (445 passed/1 skipped/0 fallos), frontend 159 tests, 3 builds limpios; sin cambios de arquitectura.

---

2026-09-14 · Fase Móvil camarero | PWA móvil: flujo completo Mesa → categoría → producto → modificadores/notas → enviar — tocar mesa libre la abre (POST /restaurant/tables/{id}/open con terminal+comensales opcionales+Idempotency-Key; abrir CREA el pedido) y entra directa al pedido; nueva pantalla TableOrderScreen con chips de categoría (GET /catalog/categories, array pelado), carta de botones grandes (GET /catalog/pos: toque añade, pulsación larga 500 ms añade con nota, pesados con Keypad de kilos) y ticket del servidor debajo (+/− en no pesables, cantidad tocable en pesables, nota editable, quitar línea vía PATCH/DELETE /sales/orders/{id}/lines); «Enviar» vuelve a Mesas (todo queda persistido línea a línea) | Decisiones: modificadores = NOTAS de línea (el backend no tiene concepto de modificadores, decisión de fase Productos; sin chips inventadas); la mesa es estado compartido → mutaciones SIEMPRE con servidor y relectura del pedido tras cada toque, SIN outbox (ese es del ticket de barra); sin conexión pantalla en lectura; CORREGIDO bug de fase 30: Mesas parseaba /restaurant/tables como array cuando devuelve {items} (la entrada de la fase anterior que decía «array directo» era errónea); waiter del seed YA tiene products.view (sin migración); supresión del click tras pulsación larga vía ref suppressClick | Falta para retomar: KDS (fase 17), impresión de comanda (tpv-agent), API de clientes (P1); suites al cierre: móvil 47 tests (9 ficheros) + build limpio, mostrador 162 tests + build limpio, backend sin cambios; sin cambios de arquitectura.

---

2026-09-14 · Fase KDS | Cuarta SPA frontend/kds (dev 5175, tema oscuro, token tpv-kds-token) sobre /api/v1/kds bajo kds.operate: tablero GET /kds/board con cabecera DERIVADA de las líneas (el cliente nunca decide estado), 4 columnas NUEVO/PREPARANDO/LISTO/SERVIDO, filtro por estación que recorta líneas, urgencias (priority 0|1), semáforo 10/20 min con aviso sonoro una vez por comanda, avance tocando la línea, botones masivos y KOT (auto por línea nueva al vender + reimpresión completa, 409 sin impresora de cocina); migración 0006_kds_stations; hooks en sales en la MISMA transacción + eventos kds.* en el tema «kds» | Decisiones: en vivo = evento ⇒ releer tablero (debounce 200 ms; el sobre nunca lleva estado) con sondeo 15 s solo sin hub; rectificación de comanda mientras pending/preparing, congelada en ready/served; quitar línea de venta BORRA la de cocina (FK sin cascade, trazabilidad en event_log) y la comanda sin líneas desaparece; CORREGIDO el handshake WS del cliente compartido — auth SIEMPRE primera trama (antes 4401 en bucle) y subscribe al «ready», fix en mostrador/móvil/kds | Falta para retomar: impresión real de KOT (tpv-agent), API de clientes (P1), kit @tpv/ui ahora justificado (ya hay 4º cliente, criterio fase 27); suites al cierre: backend 481 (480 passed/1 skipped/0 fallos), kds 23, mostrador 185, móvil 47, 4 builds limpios; ARCHITECTURE: solo §1.4 (montaje /app/kds ya real) y §3 (KitchenStation/estado derivado), por lo demás sin cambios de arquitectura.

---

2026-09-14 · Fase Revisión AGPL / reutilización URY | Verificación dirigida antes de incorporar código de URY: búsqueda de marcas ury/socket.io/qz-tray/jsrsasign/frappe en las 4 SPA + backend y dependencias exactas en los 4 lockfiles → CERO código AGPL incorporado; todas las coincidencias eran subcadenas al azar en hashes sha512 (lockfiles, bundle dist, XML de pytest), salvo la cita textual «mesas/URY-style» en la descripción del parámetro restaurant.enabled (seed.sql:116, conservada como atribución); realtime propio = WebSocket nativo y impresión = cola propia, sin nada de su stack | Decisiones: ratificado ADR-007 — sin obligaciones AGPL (ideas UX no son copyright; todo lo adoptado —sidebar categorías, semáforo mesas, filtro recién cobradas, semáforo/timers KDS, notas a cocina— reimplementado desde cero; el análisis de fase 26 se hizo sin ejecutar su DEMO); inventario por pieza con los 7 puntos del prompt en docs/agpl/REVISION-AGPL-2026-09-14.md; ADR-009 fija el checklist OBLIGATORIO para cualquier código de terceros (fichero/licencia/dependencias/copiar-modificar-estudiar/procedencia/obligaciones/ADR; con dudas → versión propia inspirada en su comportamiento) | Falta para retomar: adaptaciones futuras de URY como implementación propia y con fila nueva en el inventario: enrutado de impresoras por sala/unidad + motivo de KOT (con tpv-agent) y cliente+pax+favoritos (P1 API de clientes); sin cambios de arquitectura (única edición: índice de ADRs ahora …ADR-009); sin suites afectadas (no se tocó código).

---

2026-09-14 · Fase Fiscalidad (Veri*Factu / TicketBAI) | Plugin fiscal DESACOPLADO del motor (ADR-010, sustituye parcialmente al ADR-003): consume el outbox sale_events (fase 06) con FOR UPDATE SKIP LOCKED, transforma cada evento en UN FiscalDocument (UNIQUE sale_event_id = idempotente) y certifica vía FiscalAdapter; traza append-only fiscal_events (queued/dispatched/accepted/rejected) + un resumen audit_log por descarga (fiscal.dispatch); máquina de estados pending→sent→accepted/rejected (rejected→pending|cancelled, terminales accepted/cancelled; fallo del adaptador = pending con attempts+1, nunca revierte la venta); API /api/v1/fiscal (status/documents/dispatch) bajo fiscal.view/fiscal.dispatch; migración 0007 aditiva (fiscal_documents, fiscal_events, 3 enums, 2 permisos) | Decisiones: usuario eligió «Ninguno aún» → SIN desarrollo legal; TPV_FISCAL_PROVIDER none|verifactu|ticketbai con fail-fast al arrancar (build_fiscal_adapter), none consume eventos sin crear documentos y NO refiscaliza histórico, verifactu/ticketbai son esqueletos que fallan con FISCAL_NOT_IMPLEMENTED; dinero SIEMPRE string en el payload (§3), sin updated_at en documentos (el histórico ES fiscal_events); normativa verificada en fuentes oficiales: Veri*Factu (RD 1007/2023 + Orden HAC/1177/2024) aplazada por RDL 15/2025 a 1-1-2027 (IS) / 1-7-2027 (resto), TicketBAI en vigor en Euskadi desde 1-1-2026 (tres regímenes hermanos, uno por Diputación; Bizkaia Norma Foral 8/2023) → docs/fiscal/README.md con checklist de activación | Falta para retomar: desarrollar el adaptador real del régimen que se active (solo implementar FiscalAdapter.submit; motor/dominio/outbox/traza/auditoría ya no se tocan) tras re-verificar la fuente oficial; suites al cierre: backend 518 passed/1 skipped (485 base + 28 dominio + 6 E2E DB-gated, recuentos de seed actualizados en test_integrity: 27 permisos, 6 parámetros); ARCHITECTURE con cambios reales (§0, §1.1, §1.2, §2 componente 8, §4.1 outbox, §14, riesgos, ADR-001…ADR-010).
